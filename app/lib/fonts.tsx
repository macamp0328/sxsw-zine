import { Cutive, Roboto_Condensed } from 'next/font/google';

export const robotoCondensed = Roboto_Condensed({
  subsets: ['latin'],
});
export const cutiveMono = Cutive({ subsets: ['latin'], weight: '400' });

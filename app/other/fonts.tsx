import { Cutive, Inter, Lato, Lusitana } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] });
export const lato = Lato({
  subsets: ['latin'],
  weight: '400',
});
export const lusitana = Lusitana({
  subsets: ['latin'],
  weight: '400',
});
export const cutiveMono = Cutive({ subsets: ['latin'], weight: '400' });
